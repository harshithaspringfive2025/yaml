<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.mycompany</groupId>
    <artifactId>live</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>mule-application</packaging>

    <name>live</name>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
        <app.runtime>4.6.0</app.runtime>
        <mule.maven.plugin.version>4.3.0</mule.maven.plugin.version>

        <!-- CI/CD variables (injected via GitHub Actions) -->
        <anypoint.username>${env.ANYPOINT_USERNAME}</anypoint.username>
        <anypoint.password>${env.ANYPOINT_PASSWORD}</anypoint.password>
        <anypoint.environment>${env.ANYPOINT_ENVIRONMENT}</anypoint.environment>
        <anypoint.region>${env.ANYPOINT_REGION}</anypoint.region>

        <!-- Fail-fast defaults (optional) -->
        <failIfMissingCredentials>true</failIfMissingCredentials>

        <munit.version>3.4.0</munit.version>
    </properties>

    <build>
        <resources>
            <resource>
                <directory>${basedir}</directory>
                <includes>
                    <include>pom.xml</include>
                    <include>.project</include>
                    <include>.classpath</include>
                    <include>.settings/**</include>
                </includes>
                <targetPath>META-INF/maven/${project.groupId}/${project.artifactId}</targetPath>
            </resource>
        </resources>

        <plugins>
            <!-- Mule Maven Plugin -->
            <plugin>
                <groupId>org.mule.tools.maven</groupId>
                <artifactId>mule-maven-plugin</artifactId>
                <version>${mule.maven.plugin.version}</version>
                <extensions>true</extensions>
                <configuration>
                    <cloudHubDeployment>
                        <uri>https://eu1.anypoint.mulesoft.com</uri>
                        <username>${anypoint.username}</username>
                        <password>${anypoint.password}</password>
                        <applicationName>live</applicationName>
                        <environment>${anypoint.environment}</environment>
                        <region>${anypoint.region}</region>
                        <workers>1</workers>
                        <workerType>MICRO</workerType>
                    </cloudHubDeployment>
                    <classifier>mule-application</classifier>
                </configuration>
                <executions>
                    <!-- Optional: Fail build if creds not set -->
                    <execution>
                        <id>check-creds</id>
                        <phase>validate</phase>
                        <goals>
                            <goal>initialize</goal>
                        </goals>
                        <configuration>
                            <failIfNotSet>${failIfMissingCredentials}</failIfNotSet>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <!-- MUnit Plugin -->
            <plugin>
                <groupId>com.mulesoft.munit.tools</groupId>
                <artifactId>munit-maven-plugin</artifactId>
                <version>${munit.version}</version>
                <executions>
                    <execution>
                        <id>test</id>
                        <phase>test</phase>
                        <goals>
                            <goal>test</goal>
                            <goal>coverage-report</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <coverage>
                        <runCoverage>true</runCoverage>
                        <formats>
                            <format>html</format>
                        </formats>
                    </coverage>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <dependencies>
        <dependency>
            <groupId>org.mule.connectors</groupId>
            <artifactId>mule-http-connector</artifactId>
            <version>1.10.3</version>
            <classifier>mule-plugin</classifier>
        </dependency>
        <dependency>
            <groupId>org.mule.connectors</groupId>
            <artifactId>mule-sockets-connector</artifactId>
            <version>1.2.5</version>
            <classifier>mule-plugin</classifier>
        </dependency>
        <dependency>
            <groupId>com.mulesoft.munit</groupId>
            <artifactId>munit-runner</artifactId>
            <version>3.4.0</version>
            <classifier>mule-plugin</classifier>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.mulesoft.munit</groupId>
            <artifactId>munit-tools</artifactId>
            <version>3.4.0</version>
            <classifier>mule-plugin</classifier>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.mule.weave</groupId>
            <artifactId>assertions</artifactId>
            <version>1.2.1</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <repositories>
        <repository>
            <id>anypoint-exchange-v3</id>
            <name>Anypoint Exchange</name>
            <url>https://maven.anypoint.mulesoft.com/api/v3/maven</url>
        </repository>
        <repository>
            <id>mulesoft-releases</id>
            <name>MuleSoft Releases Repository</name>
            <url>https://repository.mulesoft.org/releases/</url>
        </repository>
    </repositories>

    <pluginRepositories>
        <pluginRepository>
            <id>mulesoft-releases</id>
            <name>MuleSoft Releases Repository</name>
            <url>https://repository.mulesoft.org/releases/</url>
            <snapshots>
                <enabled>false</enabled>
            </snapshots>
        </pluginRepository>
    </pluginRepositories>
</project>
